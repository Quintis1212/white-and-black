
arr concatenation
```

 let b =   uniqeBrandArr.map(el=>{
              return <p>{el}</p>
             })

             
let c =  uniqeSizeArr.map(el=>{
                return <p>{el}</p>
               })

               let a = [b,c]


```
